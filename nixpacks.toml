[phases.setup]
nixPkgs = ["python311", "unzip", "nodejs_20"]

[phases.install]
cmds = ["python -m venv /opt/venv", "/opt/venv/bin/pip install -r requirements.txt"]

[phases.build]
cmds = ["/opt/venv/bin/python -m reflex init"]

[start]
cmd = "/opt/venv/bin/python -m reflex run --env prod"
